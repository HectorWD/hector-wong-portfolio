export interface ContactContent {
  sectionTitle: string;
  sectionMeta: string;
  headline: string;
  body: string;
  fromLabel: string;
  fromPlaceholder: string;
  messageLabel: string;
  messagePlaceholder: string;
  submitLabel: string;
  /** Asunto prellenado del correo. */
  mailSubject: string;
  directLabel: string;
}
